# Architecture baseline

The public system name is AIMETON Cognitive Operations Complex (AIMETON.COGOPS).

The initial implementation is contract-first. Its central distinction is not four independent retrieval filters, but a deterministic compatibility registry that admits an operational ContextBundle only when mandatory cross-projection conditions are satisfied.

Architecture documents are engineering contracts. Experimental claims remain conditional on the preregistered ablation protocol and replication.
